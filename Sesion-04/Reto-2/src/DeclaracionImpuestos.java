public record DeclaracionImpuestos(String rfcContribuyente, double montoDeclarado) {
    // No se necesita implementar nada más, ya que los records manejan automáticamente los métodos equals(), hashCode() y toString().
}
